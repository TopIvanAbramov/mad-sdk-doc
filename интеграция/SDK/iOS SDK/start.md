---
label: Установка
order: 1
---

# Установка

Mad SDK — это лёгкий клиент для встраивания рекламы в мобильные приложения. Он позволяет запускать кампании без необходимости реализовывать собственную логику таргетинга, выбора и аналитики.

SDK работает по принципу "тонкого клиента": все решения принимает сервер, а SDK:

- передаёт контекст текущего экрана
- получает кампании и их содержимое в виде JSON или встроенных шаблонов
- активирует показ inline и in-app кампаний
- трекает взаимодействия пользователя (просмотры, клики, и т.д.)
- помогает фиксировать конверсии для аналитики и обучения моделей

Эта документация предназначена для мобильных разработчиков и инженеров, которые интегрируют SDK в приложение.

# 0. Информация о Mads SDK

Минимальная поддерживаемая версия: iOS 15.0+


# 1. Добавление зависимостей

- В XCode проекте необходимо добавить пакет File -> Add Package dependencies

- Укажите URL репозитория с Swift Package. Рекомендуем зафиксировать определенную версию библиотеки

```console
https://github.com/magnit-tech/mads-ios-sdk
```


![|800x200](./package.png)



Убедитесь, что MadSDK прилинковано к таргету вашего приложения

![|800x200](./linking.png)


# 2. Инициализация SDK


Прежде, чем использовать функционал SDK, интегрирующая сторона должна выполнить его инициализацию после старта приложения. Лучше всего это сделать в классе SceneDelegate или в AppDelegate.

```swift
import MadSDK

MadsSDK.initialize()
```


# 3. Идентификация пользователя

Когда пользователь входит в систему, важно связать его действия, совершенные анонимно, с его постоянным профилем. Для этого после авторизации необходимо вызывать соответствующий метод и передавать внутренний userId

```swift
MadsSDK.userId = "sample-user-id"
```


# 4. Конфигурация SDK


| Настройка параметра | Описание |
|----------|----------|----------|
| MadsSDK.isDebugLogsEnabled = true | Включает получение дебаг логов в консоль |
| MadsSDK.isDebugCreativeEnabled = true | Включает получение дебаг креатика | 



## Пример интеграции SDK

Пример интеграции SDK можно посмотреть в [репозитории на GitHub](https://github.com/magnit-tech/mads-ios-sdk)

